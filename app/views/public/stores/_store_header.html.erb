<div class="category-header">
  <nav>
    <ul style="list-style: none;">
      <% StoreCategory.all.each do |store_category| %>
        <li><h5><%= link_to store_category.name, store_stores_path(store_category_id: store_category.id), class: 'text-dark' %></h5></li>
      <% end %>
    </ul>
  </nav>
</div>